// Saber la ultima letra de un string
endsWith("")
  //Posicionar elementos
  //Poner al final
  .push("")

  //Quitar del final
  .pop("")

  // Sacar del comienzo
  .shift("")

  //Poner al comienzo
  .unshift("")

  //Recorrer elementos

  //Posicion del elemento
  .indexOf("elemento")

  //Si el elemento existe
  .includes("elemento")

  //Si la condicion se cumple
  .some((a) => a === elemento)

  //Si todos cumplen la condicion
  .every((a) => a === elemento)

  //primer elemento que cumple
  .find((a) => a === elemento)

  //Indice del primer elemento que cumple la condicion
  .findIndex((a) => a === elemento);
